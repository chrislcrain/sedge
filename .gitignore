# compiled binary at repo root (e.g. from `go build`)
/sedge

# built by the sedge-tui-test harness (bin/run.sh writes here)
/.claude/skills/sedge-tui-test/bin/sedge

# build artifacts
/dist/
/build/
/bin/

# Go test/cover output
*.test
*.out
*.cov
coverage.txt
profile.out

# Go workspace files
go.work
go.work.sum

# editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS noise
.DS_Store
Thumbs.db

# sedge-specific: per-clone agent overrides are personal
AGENTS.local.md
.sedge.local.md

# sedge writes per-worktree metadata into each worktree; never commit it
.sedge-meta.toml

# direnv
.envrc
.direnv/
